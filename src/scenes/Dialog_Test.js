let i = 0;

const testDialog = {
    name: "Test",
    [`line${++i}`]:"This is the first line of dialog",
    [`line${++i}`]:"This is the first line of dialog",
    [`line${++i}`]:"And this is the 3rd line of dialog",
};